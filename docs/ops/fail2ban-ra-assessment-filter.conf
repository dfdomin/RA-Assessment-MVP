# Copy to /etc/fail2ban/filter.d/ra-assessment.conf
# Validates login failures emitted to /var/log/ra-assessment/security.jsonl.

[Definition]
failregex = ^.*"event":\s*"login_failed".*"ip":\s*"<HOST>".*$
ignoreregex =
